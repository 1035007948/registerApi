server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: register-api
  datasource:
    url: jdbc:mysql://192.168.2.118:3306/register_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

jwt:
  secret: registerApiSecretKeyForJwtTokenAuthentication2024
  expiration: 86400000
